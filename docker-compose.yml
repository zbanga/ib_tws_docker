tws:
    build: .
    environment:
        TWSUSERID: edemo
        TWSPASSWORD: demouser
        VNC_PASSWORD: doughnuts
    ports:
        - "4001:4001"   # API
        - "5900:5900"   # VNC
    restart: always
